# Copy to deploy/.env and replace these documentation-only addresses.
SITE_IP=192.0.2.10
LAN_CIDR=192.0.2.0/24
# Optional: leave empty for IP-only URLs and no Windows DNS changes.
APP_DOMAIN=
# Required only when installing the optional LAN DNS server.
UPSTREAM_DNS=192.0.2.1
METRICS_NETWORK_INTERFACE=
